apiVersion: v1
kind: Service
metadata:
  name: clinic-web-service
  labels:
    app: release-name
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: release-name